export const metadata = {
  title: "About VOXA",
  description:
    "VOXA was built on the idea that everyday business conversations matter — and that they should produce structured action every time.",
};

export default function AboutLayout({ children }) {
  return children;
}
