export const metadata = {
  title: "Solutions | VOXA",
  description: "VOXA is built for two industries: real estate and e-commerce.",
};

export default function SolutionsLayout({ children }) {
  return children;
}
